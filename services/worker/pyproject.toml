[project]
name = "research-asset-worker"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "boto3>=1.34",
  "psycopg[binary]>=3.1",
  "redis>=5.0",
  "rq>=1.16"
]

[project.optional-dependencies]
test = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
